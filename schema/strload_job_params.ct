CREATE TABLE strload_job_params (
  id integer not null
  , redshift_ds varchar(32)
  , ctl_ds varchar(32)
  , work_table varchar(128)
  , sql_file varchar(128)
  , load_options jsonb
  , CONSTRAINT strload_job_params_pkey PRIMARY KEY (id)
);

